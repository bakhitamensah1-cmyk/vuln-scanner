# vuln-scanner

A lightweight CLI vulnerability scanner that checks files for common insecure patterns.

## Usage

Run against a directory (or file):

```bash
python3 scanner.py /path/to/project
```

Output JSON:

```bash
python3 scanner.py /path/to/project --json
```